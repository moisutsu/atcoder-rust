use proconio::*;

#[fastout]
#[allow(non_snake_case)]
fn main() {
    input! {
        N: usize, M: usize,
        mut AB: [(usize, usize); M],
    }
    let mut count = 0;
    let mut miti: Vec<usize> = vec![0; N];
}
